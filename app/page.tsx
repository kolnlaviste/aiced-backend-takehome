export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif", lineHeight: 1.5 }}>
      <h1>Take-home sandbox</h1>
      <p>
        See <code>README.md</code> for your task. API routes live under{" "}
        <code>/api</code> (e.g. <code>/api/documents</code>).
      </p>
    </main>
  );
}
